import Styles from "@/styles";
import styled from "styled-components";

export const HeaderWraper = styled.div``;

export const LinkRow = styled(Styles.Row)`
  svg {
    width: 24px;
    min-width: 24px;
    height: 24px;
    path {
      stroke: #fff;
    }
  }
`;
